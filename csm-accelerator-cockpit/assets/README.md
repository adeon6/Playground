# Assets

Optional source assets can live here.

If `guided_discovery_conversation_template.docx` is present, the cockpit parses it into the question bank. If it is absent, the app uses the built-in section list from `csm_cockpit/services.py`.

