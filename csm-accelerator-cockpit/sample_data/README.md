# Sample Data

`sanitized_geo_spatial_discovery.docx` is a sanitized demo transcript for local testing. It uses generic speakers and fictionalized content so the cockpit can be shared without exposing customer-specific transcript text.

The matching markdown source is included so the sample can be reviewed and regenerated if needed.
